#! /bin/sh -e

PREFIX="@prefix@"

JARLIBDIR="${PREFIX}/lib/cdk"
JARLIBS=`ls -1 ${JARLIBDIR}/*.jar | perl -ne "s/\n|\r/:/g; print;"`
CLASSPATH="@PATHTO3DJAR@:@PATHTORTJAR@:${JARLIBS}"

JAVA="@PATHTOJAVA@"

${JAVA} -cp ${CLASSPATH} org.openscience.cdk.applications.Viewer $*
