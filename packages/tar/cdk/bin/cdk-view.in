#! /bin/sh -e

PREFIX=@prefix@
THREEDJAR=@PATHTO3DJAR@
RTJAR=@PATHTORTJAR@
JAVA=@PATHTOJAVA@
CDKLIBDIR=@CDKLIBDIR@
CDKJARDIR=@CDKJARDIR@

JARLIBS=`ls -1 ${CDKLIBDIR}/*.jar | perl -ne "s/\n|\r/:/g; print;"`
CDKLIBS=`ls -1 ${CDKJARDIR}/*.jar | perl -ne "s/\n|\r/:/g; print;"`
CLASSPATH=${RTJAR}:${JARLIBS}:${THREEDJAR}:${CDKLIBS}

# Collect -D options as java arguments
command=${JAVA}
while [ `echo $1 | egrep '^-D' | wc -l` != 0 ]; do
        command="$command $1"
        shift
done


${command} -Dcdk.debugging=true -cp ${CLASSPATH} org.openscience.cdk.applications.Viewer $*
