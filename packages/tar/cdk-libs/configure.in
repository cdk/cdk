dnl
dnl $Id$
dnl
AC_INIT()
AM_INIT_AUTOMAKE(cdk, `cat VERSION`)

dnl Checks for programs.
AC_PROG_INSTALL
AC_PROG_LN_S
AC_PROG_MAKE_SET

AC_CHECK_PROG(HASJAVA, java, yes, no)
if test "xyes" = "x$HASJAVA"
then
        AC_PATH_PROG(PATHTOJAVA, java, no)
fi

AC_OUTPUT([
   Makefile
   jar/Makefile
])

