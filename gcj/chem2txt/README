HOWTO GET A NATIVE chem2txt PROGRAM

Ok, here's what to do:

1. cd . && ant copy
2. read FixJars.txt and run commands
3. read createSharedLibs.txt and run commands
4. copy ./jars/*.so to ./src
5. cdk src && gcj-4.0 -o chem2txt -L. -lgnujaxp -ljgrapht-0.5.3 -llog4j -lprintf -lvecmath1.2-1.14 --CLASSPATH=.:gnujaxp.jar:jgrapht-0.5.3.jar:log4j.jar:printf.jar:vecmath1.2-1.14.jar --main=org.openscience.cdk.applications.File2Text -combine `find . -name "*.java"`

It currently fails with a few errors :(

