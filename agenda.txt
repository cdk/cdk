- Port much more from JChemPaint 1.x to CDK, among which

- Implement a SMILES parser/generator pair 

- Improve CML reader:

  1. replace CDOPI layer by CDK base classes
  2. use catalog parser
  3. be namespace aware (aka "parse XHTML docs with CML fragments") 

- Add cdk-view, a viewer that detects 2D/3D coordinates and chooses
  the appropriate viewer (optionally using Java3D)

- Add cdk-merge, to merge to content interactively of two source files

- Add cdk-dbquery/cdk-dbinsert to interact with the chemical database
  (see also cdk-dbadmin)
